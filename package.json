{
  "name": "formsdk",
  "version": "1.0.0",
  "description": "Zero-lock-in, framework-agnostic Form SDK with pluggable adapters",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}